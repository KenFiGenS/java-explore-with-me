-- CREATE SCHEMA stats;

CREATE TABLE IF NOT EXISTS hits (
  HIT_ID INT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
  APP VARCHAR (255) NOT NULL,
  URI VARCHAR (255),
  IP VARCHAR (55),
  TIME_CREATE TIMESTAMP
);